 rm -R be_near
 nano docker-compose.yml
 git clone https://github.com/gradinarnn/be_near.git
 git clone https://github.com/gradinarnn/telegram_bot.git
 docker-compose up
 docker rmi -f
 docker stop $(docker container ls -aq)
 docker exec -it <id> bash



 docker build ~/telegram_bot
 docker run -d

    #находение volume
  - postgres_data:/var/lib/postgresql/data


    #удалить все volume
    docker volume prune

